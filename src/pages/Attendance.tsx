import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const Attendance = () => {
  return (
    <div className="space-y-6 p-1">
      <Card>
        <CardHeader>
          <CardTitle>Absensi</CardTitle>
        </CardHeader>
        <CardContent>
          <p className="text-muted-foreground">Halaman absensi sedang dalam pengembangan.</p>
        </CardContent>
      </Card>
    </div>
  );
};

export default Attendance;
